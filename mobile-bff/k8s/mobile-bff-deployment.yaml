apiVersion: apps/v1
kind: Deployment
metadata:
  name: mobile-bff-deployment
  namespace: bookstore-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mobile-bff
  template:
    metadata:
      labels:
        app: mobile-bff
    spec:
      containers:
      - name: mobile-bff
        image: ybhartia/mobile-bff:v1
        imagePullPolicy : Always
        ports:
        - containerPort: 80
        env:
        - name: CUSTOMER_SERVICE_URL
          value: "http://customer-service:3000"
        - name: BOOK_SERVICE_URL
          value: "http://book-service:3000"
        livenessProbe:
          httpGet:
            path: /status
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5